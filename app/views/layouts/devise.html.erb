<!DOCTYPE html>
<html>
  <head>
    <title>記事管理アプリ</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap 5.3 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </head>

  <body class="min-vh-100 d-flex align-items-center justify-content-center bg-light">

    <div class="card shadow-sm" style="width:100%;max-width:520px;border-radius:1rem;">
      <!-- タイトルヘッダ（articlesヘッダと同じ色 #6c757d） -->
      <div class="card-header text-white text-center py-3" style="background-color:#6c757d;">
        <h5 class="m-0 fw-bold">記事管理アプリ</h5>
      </div>

      <div class="card-body p-4">
        <% flash.each do |key, value| %>
          <div class="alert alert-<%= key.to_s == "notice" ? "success" : "danger" %> mb-3"><%= value %></div>
        <% end %>
        <%= yield %>
      </div>
    </div>

  </body>
</html>
